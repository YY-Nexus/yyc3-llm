# 应用 Service 草案（ClusterIP，供 Ingress/Gateway 使用）
apiVersion: v1
kind: Service
metadata:
  name: yanyu-llm-web
  namespace: yanyu-cloud
  labels:
    app: yanyu-llm-web
spec:
  type: ClusterIP
  selector:
    app: yanyu-llm-web
  ports:
    - name: http
      port: 80
      targetPort: 3000